plugins {
    id 'java'
}

group = 'top.staticplant.menu'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useJUnitPlatform()
}

repositories {
    maven { url 'https://maven.fabricmc.net/' } // Fabric官方Maven仓库
    mavenCentral() // 中央仓库，总是好的实践来包含它
}
